import React from 'react';
import { ConsentManagerCategory, ConsentManagerService } from './types';
export default function createConsentManager({ segmentWriteKey, preset, segmentServices, otherServices, categories, forceShow, }: {
    segmentWriteKey?: string;
    preset?: 'oss' | 'enterprise';
    segmentServices?: ConsentManagerService[];
    otherServices?: ConsentManagerService[];
    categories?: ConsentManagerCategory[];
    forceShow?: boolean;
}): {
    ConsentManager: React.ComponentType;
    openConsentManager: () => void;
};
