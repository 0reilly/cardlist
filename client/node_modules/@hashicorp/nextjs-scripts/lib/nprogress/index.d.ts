import { SingletonRouter } from 'next/router';
export default function NextNProgress({ Router, start, finish, error, }: {
    Router: SingletonRouter;
    start?: () => void;
    error?: () => void;
    finish?: () => void;
}): void;
